import React,{
    Component
} from 'react'

import{
    StyleSheet,
    Text,
    View
} from 'react-native'

class Albumlist extends Component{
    render(){
        return(
            <Text>Albumlist!!</Text>
        )
    }
}
export default Albumlist